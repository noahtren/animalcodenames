from setuptools import setup, find_packages
setup(
  name="animalcodenames",
  version="1.0",
  description="Generate human-readable codenames that describe colorful animals",
  packages=find_packages()
)